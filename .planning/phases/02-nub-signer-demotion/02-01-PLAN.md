---
phase: 02-nub-signer-demotion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CLAUDE.md
autonomous: true
requirements:
  - SPEC-03
  - SPEC-07

must_haves:
  truths:
    - "PR #1 (nub-signer) is closed on GitHub with an explanatory message"
    - "The remote branch origin/nub-signer no longer exists"
    - "The README.md registry table has no NUB-SIGNER row"
    - "All references to NUB-IPC in README.md and CLAUDE.md use the canonical name NUB-IFC"
    - "CLAUDE.md no longer lists NUB-SIGNER as a spec to submit"
  artifacts:
    - path: "README.md"
      provides: "Registry table without NUB-SIGNER row"
      contains: "NUB-IFC"
    - path: "CLAUDE.md"
      provides: "Updated project instructions without NUB-SIGNER, with NUB-IFC naming"
      contains: "NUB-IFC"
  key_links:
    - from: "README.md registry table"
      to: "GitHub PR #5"
      via: "NUB-IFC link in table row"
      pattern: "NUB-IFC.*pull/5"
---

<objective>
Close the NUB-SIGNER spec (PR #1) entirely and resolve the IFC/IPC naming inconsistency across the repo. NUB-SIGNER is removed because NIP-07 `window.nostr` is a core SPEC.md requirement -- there is nothing left for a separate NUB-SIGNER to specify. The canonical name NUB-IFC replaces all NUB-IPC references.

Purpose: Unblock all subsequent spec rewrites (Phases 3-7) by eliminating a dead spec and resolving naming ambiguity so downstream phases reference the correct canonical name.
Output: Closed PR, deleted branch, updated README.md and CLAUDE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-nub-signer-demotion/02-CONTEXT.md

<interfaces>
<!-- No code interfaces -- this is a spec repo. Key artifacts are markdown files and GitHub PRs. -->

Current README.md registry table (lines 18-25):
```markdown
| NUB ID | Namespace | Description | Status |
|--------|-----------|-------------|--------|
| [NUB-RELAY](https://github.com/napplet/nubs/pull/2) | `window.napplet.relay` | NIP-01 relay proxy | Draft |
| [NUB-STORAGE](https://github.com/napplet/nubs/pull/3) | `window.napplet.storage` | Scoped key-value storage | Draft |
| [NUB-SIGNER](https://github.com/napplet/nubs/pull/1) | `window.nostr` | NIP-07 signer proxy | Draft |
| [NUB-NOSTRDB](https://github.com/napplet/nubs/pull/4) | `window.nostrdb` | Local event database | Draft |
| [NUB-IFC](https://github.com/napplet/nubs/pull/5) | `window.napplet.ifc` | Inter-frame communication | Draft |
| [NUB-PIPES](https://github.com/napplet/nubs/pull/6) | `window.napplet.pipes` | Authenticated point-to-point connections | Draft |
```

GitHub state:
- PR #1 (nub-signer): OPEN, branch `nub-signer`
- PR #5 (nub-ipc): OPEN, branch `nub-ipc` (stale branch name, spec content already says NUB-IFC)
- Remote branches: origin/nub-signer, origin/nub-ipc exist
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close NUB-SIGNER PR and delete remote branch</name>
  <files>N/A (GitHub operations only)</files>
  <read_first>
    - .planning/phases/02-nub-signer-demotion/02-CONTEXT.md (decisions D-01, D-02)
  </read_first>
  <action>
Close PR #1 on napplet/nubs with a comment explaining the decision, then delete the remote branch. Per D-01 and D-02:

1. Close PR #1 with comment. The comment should explain:
   - NUB-SIGNER is being closed because NIP-07 `window.nostr` is now a core SPEC.md requirement (shells MUST provide it).
   - There is nothing left for a separate NUB-SIGNER spec to define.
   - Signing is shell-internal behavior, not a napplet-callable interface.
   - This aligns with the two-layer architecture established in SPEC.md.

   Use `gh pr close 1 --comment "..."` to close with the explanatory message.

2. Delete the remote branch `nub-signer`:
   `git push origin --delete nub-signer`

Do NOT delete any local branches or modify any local files in this task.
  </action>
  <verify>
    <automated>gh pr view 1 --json state --jq '.state' | grep -q "CLOSED" && echo "PR closed" && ! git ls-remote --heads origin nub-signer | grep -q nub-signer && echo "Branch deleted"</automated>
  </verify>
  <acceptance_criteria>
    - `gh pr view 1 --json state --jq '.state'` returns "CLOSED"
    - `gh pr view 1 --json comments --jq '.comments[-1].body'` contains "SPEC.md" and "window.nostr"
    - `git ls-remote --heads origin nub-signer` returns empty (no matching branch)
  </acceptance_criteria>
  <done>PR #1 is closed with an explanatory comment. The remote branch nub-signer no longer exists on origin.</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md and CLAUDE.md -- remove NUB-SIGNER, canonicalize NUB-IFC naming</name>
  <files>README.md, CLAUDE.md</files>
  <read_first>
    - README.md (full file -- only 62 lines)
    - CLAUDE.md (full file -- only 78 lines)
    - .planning/phases/02-nub-signer-demotion/02-CONTEXT.md (decisions D-03, D-04, D-05)
  </read_first>
  <action>
Two files need updates. Per D-03, D-04, D-05:

**README.md changes:**

1. Line 6: The word "IPC" appears in general prose ("relay access, storage, signing, IPC, pipes"). Change "IPC" to "IFC" here since NUB-IFC is the canonical name and this sentence lists NUB domains.

2. Lines 18-25 (registry table): Remove the entire NUB-SIGNER row:
   `| [NUB-SIGNER](https://github.com/napplet/nubs/pull/1) | `window.nostr` | NIP-07 signer proxy | Draft |`

   The resulting table should have 5 rows (RELAY, STORAGE, NOSTRDB, IFC, PIPES).

**CLAUDE.md changes:**

1. Line 7: Change "IPC" to "IFC" in the general prose ("relay proxy, storage, signing, IPC, pipes, message protocols"). Also consider whether "signing" should remain in this list -- it should, since signing is still a concept even though NUB-SIGNER is gone (shell handles it internally).

2. Line 27: Remove `NUB-SIGNER.md` from the example list. Change:
   `NUB-RELAY.md`, `NUB-STORAGE.md`, `NUB-SIGNER.md`, etc.`
   to:
   `NUB-RELAY.md`, `NUB-STORAGE.md`, `NUB-IFC.md`, etc.`
   (Replacing NUB-SIGNER with NUB-IFC as a representative example.)

3. Line 34: Update the spec name list. Change:
   `NUB-RELAY`, `NUB-STORAGE`, `NUB-SIGNER`, `NUB-NOSTRDB`, `NUB-IPC`, `NUB-PIPES`
   to:
   `NUB-RELAY`, `NUB-STORAGE`, `NUB-NOSTRDB`, `NUB-IFC`, `NUB-PIPES`
   (Remove NUB-SIGNER entirely, rename NUB-IPC to NUB-IFC.)

4. Lines 63-72 (PR table): Remove the NUB-SIGNER row entirely:
   `| NUB-SIGNER | ~/Develop/napplet/specs/nubs/NUB-SIGNER.md | nub-signer |`

5. Line 70: Rename NUB-IPC to NUB-IFC in the PR table:
   Change: `| NUB-IPC | ~/Develop/napplet/specs/nubs/NUB-IPC.md | nub-ipc |`
   To: `| NUB-IFC | ~/Develop/napplet/specs/nubs/NUB-IFC.md | nub-ifc |`

After edits, verify no remaining occurrences of "NUB-SIGNER" or "NUB-IPC" exist in either file (general terms "IPC", "signer", "signing" in prose are acceptable -- only the spec identifiers "NUB-SIGNER" and "NUB-IPC" must be gone).
  </action>
  <verify>
    <automated>! grep -q "NUB-SIGNER" README.md && ! grep -q "NUB-IPC" README.md && ! grep -q "NUB-SIGNER" CLAUDE.md && ! grep -q "NUB-IPC" CLAUDE.md && grep -q "NUB-IFC" README.md && grep -q "NUB-IFC" CLAUDE.md && echo "All naming verified"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "NUB-SIGNER" README.md` returns 0
    - `grep -c "NUB-IPC" README.md` returns 0
    - `grep -c "NUB-SIGNER" CLAUDE.md` returns 0
    - `grep -c "NUB-IPC" CLAUDE.md` returns 0
    - `grep -c "NUB-IFC" README.md` returns at least 1
    - `grep -c "NUB-IFC" CLAUDE.md` returns at least 1
    - README.md registry table has exactly 5 data rows (no NUB-SIGNER)
    - CLAUDE.md PR table has exactly 5 data rows (no NUB-SIGNER)
  </acceptance_criteria>
  <done>README.md registry table has no NUB-SIGNER row and uses NUB-IFC consistently. CLAUDE.md has no NUB-SIGNER references and all NUB-IPC references replaced with NUB-IFC.</done>
</task>

</tasks>

<verification>
Phase-level verification after both tasks complete:

1. **NUB-SIGNER is fully removed from the project:**
   - `gh pr view 1 --json state` shows CLOSED
   - `git ls-remote --heads origin nub-signer` returns empty
   - `grep -r "NUB-SIGNER" README.md CLAUDE.md` returns nothing

2. **NUB-IFC naming is canonical everywhere:**
   - `grep -r "NUB-IPC" README.md CLAUDE.md` returns nothing
   - `grep "NUB-IFC" README.md` shows the registry table row
   - `grep "NUB-IFC" CLAUDE.md` shows updated spec references

3. **No collateral damage:**
   - README.md still has 5 spec rows in registry table
   - CLAUDE.md still has 5 spec rows in PR table
   - All other content in both files is preserved
</verification>

<success_criteria>
- PR #1 (NUB-SIGNER) is closed with explanatory comment
- Remote branch `nub-signer` is deleted
- README.md has no NUB-SIGNER row, uses NUB-IFC (not NUB-IPC)
- CLAUDE.md has no NUB-SIGNER references, uses NUB-IFC (not NUB-IPC)
- No remaining "NUB-SIGNER" or "NUB-IPC" strings in tracked markdown files
</success_criteria>

<output>
After completion, create `.planning/phases/02-nub-signer-demotion/02-01-SUMMARY.md`
</output>
