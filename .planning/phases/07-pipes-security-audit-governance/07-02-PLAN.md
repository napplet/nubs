---
phase: 07-pipes-security-audit-governance
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - NUB-IFC.md (on nub-ipc branch, security section only)
  - TEMPLATE-WORD.md
  - TEMPLATE-NN.md
  - README.md
autonomous: true
requirements: [SPEC-08, GOVN-01, GOVN-02, GOVN-03]

must_haves:
  truths:
    - "No spec says napplets MUST sign or verify — all crypto MUST clauses have the shell/runtime as subject"
    - "No stale NUB-PIPES references in any of the 4 remaining specs"
    - "TEMPLATE-WORD.md uses wire format structure (type strings, payload shapes, shell behavior)"
    - "TEMPLATE-NN.md references SPEC.md wire format, no NIP-01 event kind references"
    - "README.md registry has exactly 4 rows: RELAY, STORAGE, NOSTRDB, IFC"
  artifacts:
    - path: "TEMPLATE-WORD.md"
      provides: "Updated interface spec template"
      contains: "Wire Protocol"
    - path: "TEMPLATE-NN.md"
      provides: "Updated message protocol template"
      contains: "wire format"
    - path: "README.md"
      provides: "Updated registry table"
  key_links:
    - from: "TEMPLATE-WORD.md"
      to: "SPEC.md wire format"
      via: "template section structure"
      pattern: "domain\\.action"
---

<objective>
Cross-spec security audit across all 4 rewritten NUB specs, update both templates to reflect the new wire format architecture, and clean up the README registry table.

Purpose: Ensures consistency across all specs (no stale crypto/PIPES refs), provides correct templates for future NUB authors, and reflects the final 4-spec registry.
Output: Security fixes pushed to spec branches, TEMPLATE-WORD.md and TEMPLATE-NN.md updated, README.md registry cleaned.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@SPEC.md

Prior plan context (channel support added to IFC):
@.planning/phases/07-pipes-security-audit-governance/07-01-SUMMARY.md

<interfaces>
<!-- Current TEMPLATE-WORD.md sections (to be replaced): -->
- Description
- API Surface
- Shell Behavior
- Event Kinds (STALE - uses kind numbers, must be replaced with Wire Protocol)
- Security Considerations
- Implementations

<!-- Current TEMPLATE-NN.md sections (to be replaced): -->
- Description
- Event Semantics (STALE - uses NIP-01 event structure with kind/tags/content)
- Negotiation
- Implementations

<!-- README.md registry currently has 5 rows including NUB-PIPES (to become 4) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-spec security audit and fix stale references</name>
  <files>NUB-IFC.md (on nub-ipc branch)</files>
  <read_first>
  - Run grep across all 4 spec branches for stale references:
    ```
    for branch in nub-relay nub-storage nub-nostrdb nub-ipc; do
      echo "=== $branch ===";
      git show $branch:NUB-*.md 2>/dev/null | grep -in -E "NUB-PIPES|NUB-SIGNER|kind [0-9]{4,}|delegated.*key|Schnorr|per-message sign";
    done
    ```
  - Also verify each spec's Security Considerations has the shell/runtime as subject of MUST clauses:
    ```
    for branch in nub-relay nub-storage nub-nostrdb nub-ipc; do
      echo "=== $branch Security ===";
      git show $branch:NUB-*.md 2>/dev/null | grep -A2 "MUST" | head -20;
    done
    ```
  </read_first>
  <acceptance_criteria>
  - For each of nub-relay, nub-storage, nub-nostrdb, nub-ipc branches:
    - `git show $branch:NUB-*.md | grep -c "NUB-PIPES"` returns 0
    - `git show $branch:NUB-*.md | grep -c "NUB-SIGNER"` returns 0
    - `git show $branch:NUB-*.md | grep -ic "napplet.*MUST.*sign\|napplet.*MUST.*verify"` returns 0 (no spec says napplets MUST sign/verify — NUB-RELAY's "napplet MUST sign via window.nostr.signEvent" is a SHOULD-level convenience, not a security MUST)
  </acceptance_criteria>
  <action>
  Per D-10 and D-11, audit all 4 specs on their branches for:

  **Stale references to grep for:**
  - `NUB-PIPES` — must be zero in all specs (PIPES is absorbed into IFC)
  - `NUB-SIGNER` — must be zero (closed in Phase 2)
  - `kind [0-9]{4,}` — no 4+ digit kind numbers in napplet-facing sections
  - `Schnorr` — should only appear as "no Schnorr signing" context, not as requirement
  - `delegated.*key` — stale crypto concept

  **Security Considerations audit (per D-11):**
  Verify in each spec that MUST clauses for crypto have the shell/runtime as subject:
  - CORRECT: "The shell MUST verify event signatures"
  - INCORRECT: "Napplets MUST sign events" (this is a spec-level requirement about workflow, not security)

  **Known issues from pre-scan:**
  1. **NUB-IFC (nub-ipc branch)**: Contains NUB-PIPES references at end of file (the "Relationship to NUB-PIPES" section was replaced with "Channels vs Topics" in Plan 01, but confirm). If any remain, remove them.
  2. **NUB-RELAY**: References to signing are CORRECT (napplets sign via window.nostr.signEvent before publish). The Security section says "The shell MUST verify event signatures" — correct subject. The note about "napplets own their signing" is about the NIP-07 flow, not a crypto MUST on napplets. No changes needed if audit confirms.
  3. **NUB-NOSTRDB**: References to signatures are CORRECT (externally-authored events validated by shell). "The shell MUST validate event signatures on nostrdb.add" — correct subject. No changes needed if audit confirms.
  4. **NUB-STORAGE**: Clean in pre-scan — confirm zero issues.

  **If any issues found**, fix them on the appropriate branch:
  - `git checkout <branch>`
  - Edit the NUB-*.md file
  - `git commit -am "fix: remove stale <reference> from NUB-<NAME>"`
  - `git push --force-with-lease origin <branch>`
  - Return to master: `git checkout master`

  **If NUB-IFC still has NUB-PIPES refs** (should have been cleaned in Plan 01):
  - Checkout nub-ipc, remove references, commit, push, return to master

  The audit is complete when all 4 specs pass the acceptance criteria grep checks.
  </action>
  <verify>
    <automated>for branch in nub-relay nub-storage nub-nostrdb nub-ipc; do count=$(git show $branch:NUB-*.md 2>/dev/null | grep -c "NUB-PIPES\|NUB-SIGNER" || true); echo "$branch: $count stale refs"; if [ "$count" -gt 0 ]; then exit 1; fi; done && echo "AUDIT PASSED"</automated>
  </verify>
  <done>All 4 specs on their branches have zero NUB-PIPES references, zero NUB-SIGNER references, and all Security Considerations MUST clauses for crypto use the shell/runtime as subject</done>
</task>

<task type="auto">
  <name>Task 2: Update TEMPLATE-WORD.md, TEMPLATE-NN.md, and README.md registry</name>
  <files>TEMPLATE-WORD.md, TEMPLATE-NN.md, README.md</files>
  <read_first>
  - Ensure on master branch: `git checkout master`
  - Read current TEMPLATE-WORD.md, TEMPLATE-NN.md, README.md
  </read_first>
  <acceptance_criteria>
  - `grep -c "Wire Protocol" TEMPLATE-WORD.md` returns >= 1
  - `grep -c "Event Kinds" TEMPLATE-WORD.md` returns 0 (old section removed)
  - `grep -c "domain.action" TEMPLATE-WORD.md` returns >= 1
  - `grep -c "wire format" TEMPLATE-NN.md` returns >= 1 (case insensitive)
  - `grep -c '"kind"' TEMPLATE-NN.md` returns 0 (no NIP-01 kind references in event structure)
  - `grep -c "NUB-PIPES" README.md` returns 0
  - `grep -c "NUB-RELAY" README.md` returns >= 1
  - `grep -c "NUB-STORAGE" README.md` returns >= 1
  - `grep -c "NUB-NOSTRDB" README.md` returns >= 1
  - `grep -c "NUB-IFC" README.md` returns >= 1
  </acceptance_criteria>
  <action>
  **A. Rewrite TEMPLATE-WORD.md (per D-12):**

  Replace the entire file with the updated template structure. The new sections are: Description, API Surface, Wire Protocol, Shell Behavior, Security Considerations, Implementations.

  ```markdown
  NUB-{NAME}
  ==========

  {Title}
  -------

  `draft`

  **NUB ID:** NUB-{NAME}
  **Namespace:** `window.napplet.{name}`
  **Discovery:** `shell.supports("{name}")`

  ## Description

  {One paragraph: what this interface provides and why a napplet needs it. State what the shell provides and what the napplet consumes.}

  ## API Surface

  {SDK-level method signatures. These are the high-level operations napplets call. Each method corresponds to one or more wire protocol messages.}

  ```typescript
  interface Napplet{Name} {
    method(param: type): ReturnType;
  }
  ```

  {Brief description of each method: what it does, what it returns, error conditions.}

  ## Wire Protocol

  {name}.* messages use the NIP-5D wire format (`{ "type": "domain.action", ...payload }`).

  | Type | Direction | Payload fields |
  |------|-----------|----------------|
  | `{name}.action` | napplet -> shell | `id`, {fields} |
  | `{name}.action.result` | shell -> napplet | `id`, {fields} |

  Key design notes:
  - Request/result pairs use `id` for correlation.
  - Fire-and-forget messages have no `id` field.
  - Shell-initiated deliveries use domain-specific routing fields.

  ### Examples

  **{Action name}:**
  ```
  -> { "type": "{name}.action", "id": "a1", {fields} }
  <- { "type": "{name}.action.result", "id": "a1", {fields} }
  ```

  ### Error Handling

  {How errors are reported — typically an `error` field in result messages.}

  ## Shell Behavior

  - The shell MUST {primary obligation}.
  - The shell MUST respond to every request with a result message carrying the same `id`.
  - The shell MAY {optional behavior}.
  - The shell MAY enforce ACL checks on {name} capabilities.

  ## Security Considerations

  {Interface-specific security requirements. The shell/runtime is the subject of MUST clauses for any crypto or trust operations — napplets do not sign or verify.}

  - {Isolation guarantees}
  - {ACL considerations}
  - {Trust boundaries}

  ## Implementations

  - {links to implementations}
  ```

  **B. Rewrite TEMPLATE-NN.md (per D-13):**

  Replace the entire file with the updated template. Remove NIP-01 event kind structure, add wire format references, update discovery pattern.

  ```markdown
  NUB-{NN}
  ========

  {Title}
  -------

  `draft`

  **NUB ID:** NUB-{NN}
  **Domain:** {e.g., feed rendering, chat, collaborative editing}
  **Requires:** {NUB-WORD interfaces needed, e.g., NUB-RELAY, NUB-IFC}
  **Discovery:** `shell.supports("{word}", "{nn}")`

  ## Description

  {One paragraph: what message semantics this protocol defines and for what purpose. Describe the coordination pattern between napplets.}

  ## Message Protocol

  Napplets coordinate using messages published and received via NUB-RELAY or NUB-IFC. Messages follow the NIP-5D wire format (`{ "type": "domain.action", ...payload }`). The protocol defines the semantic meaning of message content — what napplets agree on when they exchange data.

  ### {Message Name}

  {Description of this message type: when it is sent, what it means, who produces and consumes it.}

  **Relay event structure** (published via `relay.publish`):
  ```json
  {
    "kind": {kind},
    "content": "{content description}",
    "tags": [{tag structure}]
  }
  ```

  {Behavioral requirements for producers and consumers.}

  ## Negotiation

  Napplets discover peers supporting this protocol via `shell.supports("{word}", "{nn}")`. A napplet requiring this protocol declares it in its manifest:

  ```
  ["requires", "{word}"]
  ```

  The protocol number is negotiated at runtime — the manifest declares the interface dependency, and napplets check for protocol support via `shell.supports()`.

  ## Implementations

  - {links to implementations}
  ```

  **C. Update README.md registry (per D-02, D-14):**

  Remove the NUB-PIPES row from the registry table. The table should have exactly 4 rows:

  | NUB ID | Namespace | Description | Status |
  |--------|-----------|-------------|--------|
  | [NUB-RELAY](https://github.com/napplet/nubs/pull/2) | `window.napplet.relay` | NIP-01 relay proxy | Draft |
  | [NUB-STORAGE](https://github.com/napplet/nubs/pull/3) | `window.napplet.storage` | Scoped key-value storage | Draft |
  | [NUB-NOSTRDB](https://github.com/napplet/nubs/pull/4) | `window.nostrdb` | Local event database | Draft |
  | [NUB-IFC](https://github.com/napplet/nubs/pull/5) | `window.napplet.ifc` | Inter-frame communication | Draft |

  Also update the NUB-IFC description from "Inter-frame communication" to "Inter-frame communication" (keep as is — channels are part of IFC now, no name change needed).

  **D. Commit all three files on master:**
  ```
  git add TEMPLATE-WORD.md TEMPLATE-NN.md README.md
  git commit -m "docs: update templates and registry for wire format architecture"
  git push origin master
  ```
  </action>
  <verify>
    <automated>grep -c "Wire Protocol" TEMPLATE-WORD.md && grep -c "Event Kinds" TEMPLATE-WORD.md && grep -c "NUB-PIPES" README.md && grep -c "NUB-RELAY" README.md</automated>
  </verify>
  <done>
  TEMPLATE-WORD.md has Wire Protocol section (no Event Kinds), TEMPLATE-NN.md uses wire format (no NIP-01 kind structure), README.md has 4 registry rows (no PIPES), all on master and pushed
  </done>
</task>

</tasks>

<verification>
- Security audit: `for branch in nub-relay nub-storage nub-nostrdb nub-ipc; do git show $branch:NUB-*.md 2>/dev/null | grep -c "NUB-PIPES\|NUB-SIGNER"; done` — all return 0
- TEMPLATE-WORD.md: `grep "Wire Protocol" TEMPLATE-WORD.md` matches, `grep "Event Kinds" TEMPLATE-WORD.md` does not match
- TEMPLATE-NN.md: `grep -i "wire format" TEMPLATE-NN.md` matches, `grep '"kind"' TEMPLATE-NN.md` does not match
- README.md: `grep -c "NUB-PIPES" README.md` returns 0, registry table has 4 rows
</verification>

<success_criteria>
All 4 specs pass security audit (no stale crypto refs, no PIPES/SIGNER refs, shell is subject of crypto MUST clauses). TEMPLATE-WORD.md and TEMPLATE-NN.md reflect wire format architecture. README.md registry has 4 specs.
</success_criteria>

<output>
After completion, create `.planning/phases/07-pipes-security-audit-governance/07-02-SUMMARY.md`
</output>
