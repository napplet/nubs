---
phase: 04-nub-relay-rewrite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [NUB-RELAY.md]
autonomous: true
requirements: [SPEC-01]

must_haves:
  truths:
    - "NUB-RELAY.md uses relay.* message types with JSON payloads — no NIP-01 wire verbs (REQ/EVENT/CLOSE/EOSE/OK) or kind 29001"
    - "relay.publish accepts a signed NostrEvent — napplets sign via window.nostr.signEvent() before publishing, no unsigned-template path"
    - "Wire Protocol section defines relay.subscribe, relay.publish, relay.query, relay.close, relay.event, relay.eose, relay.closed, relay.publish.result"
    - "Shell Behavior describes how the runtime forwards signed events to relays and delivers incoming events — this is not visible to napplets beyond the message types"
    - "PR #2 on GitHub reflects the rewritten spec"
  artifacts:
    - path: "NUB-RELAY.md"
      provides: "Rewritten NUB-RELAY spec using SPEC.md wire format"
      contains: "relay.subscribe"
  key_links:
    - from: "NUB-RELAY.md Wire Protocol"
      to: "NUB-RELAY.md API Surface"
      via: "message type names mirror API methods"
      pattern: "relay\\.(subscribe|publish|query|close)"
    - from: "NUB-RELAY.md Wire Protocol"
      to: "SPEC.md wire format"
      via: "domain.action pattern with .result suffix"
      pattern: "relay\\.publish\\.result"
    - from: "NUB-RELAY.md API Surface"
      to: "SPEC.md line 33"
      via: "window.nostr.signEvent() for signing before relay.publish"
      pattern: "window\\.nostr\\.signEvent"
---

<objective>
Rewrite NUB-RELAY spec on the `nub-relay` PR branch to replace NIP-01 wire verbs (REQ/EVENT/CLOSE/EOSE/OK) and kind 29001 scoped relay events with SPEC.md's `{ "type": "relay.*", ...payload }` format. The API surface (subscribe/publish/query) is preserved but `publish()` now accepts a signed NostrEvent (napplets sign via `window.nostr.signEvent()` first). This is the highest-impact spec rewrite — relay access is the most fundamental shell capability.

Purpose: Align NUB-RELAY with the two-layer architecture from SPEC.md. The napplet-facing API uses typed messages; the shell internally handles relay pool management and forwarding. Napplets own their signing via NIP-07.
Output: Rewritten NUB-RELAY.md force-pushed to `nub-relay` branch (PR #2), PR body updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-nub-relay-rewrite/04-CONTEXT.md
@SPEC.md

<interfaces>
<!-- SPEC.md wire format pattern (canonical): -->

Wire format: { "type": "<domain>.<action>", ...payload }
Response pattern: { "type": "<domain>.<action>.result", "id": "<correlation>", ...result }
Example from SPEC.md:
  Request:  { "type": "foo.bar", "id": "abc", "data": {...} }
  Response: { "type": "foo.bar.result", "id": "abc", "result": {...} }

Identity: shell maps MessageEvent.source to (dTag, aggregateHash) at iframe creation
Discovery: shell.supports("relay")
Namespace: window.napplet.relay

<!-- NUB-STORAGE established patterns (from nub-storage branch): -->

Section order: Description -> API Surface -> Wire Protocol -> Shell Behavior -> Security Considerations
TypeScript interface block with // via domain.action annotations
Wire protocol table: Type | Direction | Payload fields
Result suffix: domain.action.result
Inline error handling via error field in result messages
Correlation ID: id field on all request/result pairs
Concrete JSON examples with -> (napplet to shell) and <- (shell to napplet) notation

<!-- Key RELAY-specific decisions (from 04-CONTEXT.md): -->

D-07: relay.publish accepts a signed NostrEvent, NOT an unsigned template.
       Napplets ALWAYS sign via window.nostr.signEvent(template) before publishing.
       One path only — no dual-path signed vs unsigned.
D-05: id field on request/result pairs, subId for subscription-scoped messages (relay.event, relay.eose, relay.closed)
D-09: Scoped relay support via relay field in messages
</interfaces>

<!-- Prior phase SUMMARY for pattern reference -->
@.planning/phases/03-nub-storage-rewrite/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite NUB-RELAY.md on nub-relay branch</name>
  <files>NUB-RELAY.md</files>
  <read_first>
    - SPEC.md (wire format pattern, identity model, line 33 re: window.nostr)
    - `git show nub-relay:NUB-RELAY.md` (existing spec to rewrite)
    - `git show nub-storage:NUB-STORAGE.md` (validated document structure pattern)
    - .planning/phases/04-nub-relay-rewrite/04-CONTEXT.md (locked decisions D-01 through D-11)
  </read_first>
  <action>
    Check out the `nub-relay` branch (stash any uncommitted changes on master first if needed). Rewrite `NUB-RELAY.md` completely. The new spec preserves the subscribe/publish/query API surface but replaces all NIP-01 wire verbs and kind 29001 with relay.* typed messages, and changes publish() to accept a signed NostrEvent.

    **Header (preserve structure, update metadata):**
    - Title: `NUB-RELAY` (keep the `=====` underline style)
    - Subtitle: Change from "NIP-01 Relay Proxy" to "Relay Proxy" (relay.* types replace NIP-01 verbs)
    - Keep: `draft` status
    - Keep: NUB ID (`NUB-RELAY`), Namespace (`window.napplet.relay`), Discovery (`shell.supports("relay")`)

    **Description section:**
    - Preserve the core explanation: sandboxed iframes cannot open WebSocket connections (no `allow-same-origin`), so the shell acts as a relay proxy.
    - Remove any reference to "REQ/EVENT/CLOSE messages" — replace with "typed relay messages".
    - Keep: "This is the most fundamental shell capability."

    **API Surface section (per D-11, D-07):**
    - TypeScript interface block with `// via relay.*` annotations:

      ```typescript
      interface NappletRelay {
        subscribe(                                        // via relay.subscribe
          filters: NostrFilter | NostrFilter[],
          options?: { relay?: string },
        ): Subscription;

        publish(event: NostrEvent): Promise<void>;       // via relay.publish

        query(                                            // via relay.query
          filters: NostrFilter | NostrFilter[],
        ): Promise<NostrEvent[]>;
      }

      interface Subscription {
        on(event: 'event', cb: (event: NostrEvent) => void): void;
        on(event: 'eose', cb: () => void): void;
        close(): void;                                    // via relay.close
      }
      ```

    - Method descriptions below the interface block:

      **`subscribe(filters, options?)`** — Opens a live subscription. The shell queries its relay pool, streaming matching events back as `relay.event` messages. Returns a handle to listen for events and EOSE, and to close the subscription. When `options.relay` is provided, the subscription targets a specific relay (e.g., for NIP-29 group relays) instead of the shared pool. (per D-01, D-09)

      **`publish(event)`** — Publishes a signed Nostr event to the shell's relay pool. The napplet MUST sign the event via `window.nostr.signEvent(template)` before calling publish. The shell forwards the signed event to relays and returns the result. (per D-07)

      **`query(filters)`** — Convenience wrapper: subscribes, collects events until EOSE, then closes the subscription and resolves the Promise with the collected events. (per D-01)

    - After the method descriptions, add a note: "All methods are async because they cross the postMessage boundary. Requests include correlation IDs; the shell responds with matching IDs so the shim can resolve the correct Promise or route events to the correct subscription." (per D-05)

    **Wire Protocol section (replaces "Event Kinds" and NIP-01 verbs — per D-01 through D-06, D-08, D-09):**
    - Section title: `## Wire Protocol`
    - Opening sentence: "Relay operations use the NIP-5D wire format. Requests include an `id` field for correlation. Subscription-scoped messages use a `subId` field to identify which subscription they belong to."
    - Define these message types as a table:

      | Type | Direction | Payload fields |
      |------|-----------|----------------|
      | `relay.subscribe` | napplet -> shell | `id`, `subId`, `filters` (NostrFilter array), `relay`? (string) |
      | `relay.close` | napplet -> shell | `id`, `subId` |
      | `relay.publish` | napplet -> shell | `id`, `event` (signed NostrEvent) |
      | `relay.query` | napplet -> shell | `id`, `filters` (NostrFilter array) |
      | `relay.event` | shell -> napplet | `subId`, `event` (NostrEvent) |
      | `relay.eose` | shell -> napplet | `subId` |
      | `relay.closed` | shell -> napplet | `subId`, `reason`? (string) |
      | `relay.publish.result` | shell -> napplet | `id`, `ok` (boolean), `eventId`? (string), `error`? (string) |
      | `relay.query.result` | shell -> napplet | `id`, `events` (NostrEvent array) |

    - After the table, a subsection `### NostrFilter` describing filter objects per D-08:
      "Filters follow the NIP-01 filter format:"
      ```json
      { "kinds": [1], "authors": ["<hex>"], "#e": ["<hex>"], "#p": ["<hex>"], "since": 1234, "until": 5678, "limit": 50 }
      ```
      "All filter fields are optional. Multiple filters in the `filters` array are OR-combined."

    - After filters, a subsection `### Examples` with concrete JSON for the three main operations:

      **Subscribe:**
      ```
      -> { "type": "relay.subscribe", "id": "a1", "subId": "sub-1", "filters": [{ "kinds": [1], "limit": 50 }] }
      <- { "type": "relay.event", "subId": "sub-1", "event": { "id": "abc...", "pubkey": "def...", "kind": 1, "content": "hello", "tags": [], "created_at": 1234567890, "sig": "..." } }
      <- { "type": "relay.eose", "subId": "sub-1" }
      ```

      **Publish (napplet signs first, then publishes):**
      ```
      // Step 1: Napplet signs the event via NIP-07
      const signed = await window.nostr.signEvent({ kind: 1, content: "hello world", tags: [], created_at: Math.floor(Date.now()/1000) });

      // Step 2: Napplet sends the signed event to the shell
      -> { "type": "relay.publish", "id": "b2", "event": { "id": "abc...", "pubkey": "def...", "kind": 1, "content": "hello world", "tags": [], "created_at": 1234567890, "sig": "ghi..." } }
      <- { "type": "relay.publish.result", "id": "b2", "ok": true, "eventId": "abc..." }
      ```

      **Query:**
      ```
      -> { "type": "relay.query", "id": "c3", "filters": [{ "authors": ["def..."], "kinds": [0], "limit": 1 }] }
      <- { "type": "relay.query.result", "id": "c3", "events": [{ "id": "xyz...", "pubkey": "def...", "kind": 0, "content": "{...}", "tags": [], "created_at": 1234567890, "sig": "..." }] }
      ```

      **Close subscription:**
      ```
      -> { "type": "relay.close", "id": "d4", "subId": "sub-1" }
      ```

      **Scoped relay (per D-09):**
      ```
      -> { "type": "relay.subscribe", "id": "e5", "subId": "sub-2", "filters": [{ "kinds": [9, 10, 11, 12] }], "relay": "wss://groups.example.com" }
      ```

      **Error in publish:**
      ```
      <- { "type": "relay.publish.result", "id": "b2", "ok": false, "error": "blocked: content policy violation" }
      ```

    - After examples, a subsection `### Error Handling`:
      "`relay.publish.result` includes `ok: false` and an `error` string on failure. `relay.closed` indicates the shell terminated a subscription, with an optional `reason`. `relay.query.result` MAY include an `error` field instead of `events` if the query fails."

    **Shell Behavior section (per D-09, D-10):**
    - The shell MUST forward subscriptions to its relay pool and deliver matching events as `relay.event` messages to the subscribing napplet.
    - The shell MUST send `relay.eose` when stored events for a subscription are exhausted.
    - The shell MUST honor `relay.close` by tearing down the subscription and ceasing event delivery for that `subId`.
    - The shell MUST forward signed events from `relay.publish` to its relay pool and respond with `relay.publish.result`.
    - The shell MUST verify event signatures before forwarding to relays. Events with invalid signatures MUST be rejected with `relay.publish.result { ok: false, error: "invalid signature" }`.
    - The shell MUST respond to `relay.query` by collecting events until EOSE and returning them in `relay.query.result`.
    - The shell MUST respond to every request with a result or lifecycle message carrying the same `id` or `subId`.
    - The shell MAY send `relay.closed` to indicate a subscription was terminated by the shell (e.g., due to an error, resource limit, or policy).
    - The shell MAY enforce ACL checks on relay read and relay write capabilities before processing subscribe or publish messages.
    - The shell MAY support scoped relay connections when the `relay` field is present, targeting a specific relay independently from the shared pool.
    - The shell MAY manage a relay pool internally — which relays to connect to, reconnection strategy, and load balancing are implementation details.

    **Security Considerations section:**
    - The shell controls which relays the napplet can access. Napplets cannot open arbitrary WebSocket connections.
    - The shell MUST verify event signatures before relay broadcast. Events with invalid signatures MUST be rejected.
    - The shell MAY filter or reject subscriptions and publishes based on ACL capabilities.
    - Scoped relay URLs SHOULD be validated by the shell to prevent SSRF-like abuse (e.g., napplets targeting internal network addresses via `wss://`).
    - Subscription filters SHOULD be validated to prevent resource exhaustion (e.g., unbounded subscriptions without `limit`).
    - The napplet signs events via `window.nostr.signEvent()` (NIP-07, provided by the shell per NIP-5D). The shell does not sign on behalf of the napplet for relay publishes — napplets own their signing.

    **Remove these sections/content entirely:**
    - "Event Kinds" table (replaced by Wire Protocol)
    - All NIP-01 wire verbs in brackets: `["REQ", ...]`, `["EVENT", ...]`, `["CLOSE", ...]`, `["EOSE", ...]`, `["OK", ...]`, `["CLOSED", ...]`, `["NOTICE", ...]`
    - Kind 29001 scoped relay events and `t` tag references
    - Any mention of `EventTemplate` as a publish input — publish takes a signed `NostrEvent` only
    - The word "NUB-SIGNER" (per D-10 context: NUB-RELAY does not reference NUB-SIGNER as a napplet-visible dependency)
    - "Implementations" section (no implementations exist yet)

    **Section ordering per D-10:** Description -> API Surface -> Wire Protocol -> Shell Behavior -> Security Considerations

    The file MUST NOT contain:
    - Any `["REQ"`, `["EVENT"`, `["CLOSE"`, `["EOSE"`, `["OK"`, `["CLOSED"`, `["NOTICE"` wire verbs
    - Any reference to kind 29001 or `t` tags
    - The string "NUB-SIGNER"
    - The string "EventTemplate" as a publish input
    - The word "delegated" (from old security section)
  </action>
  <verify>
    <automated>git diff nub-relay -- NUB-RELAY.md | head -200 && echo "---COUNTS---" && echo "relay.subscribe count:" && grep -c "relay\.subscribe" NUB-RELAY.md && echo "relay.publish count:" && grep -c "relay\.publish" NUB-RELAY.md && echo "relay.event count:" && grep -c "relay\.event" NUB-RELAY.md && echo "forbidden terms:" && grep -ci 'kind 29001\|29001\|\["REQ"\|\["EVENT"\|\["CLOSE"\|\["EOSE"\|\["OK"\|\["CLOSED"\|\["NOTICE"\|NUB-SIGNER\|EventTemplate\|delegated' NUB-RELAY.md || echo "0 forbidden terms"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "relay\.subscribe" NUB-RELAY.md` returns 4 or more (table, example, method desc, interface)
    - `grep -c "relay\.publish" NUB-RELAY.md` returns 5 or more (table x2 counting .result, example, method desc, interface)
    - `grep -c "relay\.query" NUB-RELAY.md` returns 3 or more (table, example, method desc)
    - `grep -c "relay\.close" NUB-RELAY.md` returns 3 or more (table, example, interface)
    - `grep -c "relay\.event" NUB-RELAY.md` returns 3 or more (table, example, shell behavior)
    - `grep -c "relay\.eose" NUB-RELAY.md` returns 2 or more (table, example)
    - `grep -c "relay\.publish\.result" NUB-RELAY.md` returns 3 or more (table, example, error example)
    - `grep -c "relay\.query\.result" NUB-RELAY.md` returns 2 or more (table, example)
    - `grep -c "relay\.closed" NUB-RELAY.md` returns 2 or more (table, shell behavior)
    - `grep -c "subId" NUB-RELAY.md` returns 5 or more (table rows, examples)
    - `grep -c "window\.nostr\.signEvent" NUB-RELAY.md` returns 2 or more (API desc, example, security)
    - `grep -ci 'kind 29001\|29001' NUB-RELAY.md` returns 0
    - `grep -ci '\["REQ"\|\["EVENT"\|\["CLOSE"\|\["EOSE"\|\["OK"\|\["CLOSED"\|\["NOTICE"' NUB-RELAY.md` returns 0
    - `grep -ci 'NUB-SIGNER' NUB-RELAY.md` returns 0
    - `grep -ci 'EventTemplate' NUB-RELAY.md` returns 0
    - `grep -ci 'delegated' NUB-RELAY.md` returns 0
    - `grep -c "## Wire Protocol" NUB-RELAY.md` returns 1
    - `grep -c "## Event Kinds" NUB-RELAY.md` returns 0
    - `grep -c "## Shell Behavior" NUB-RELAY.md` returns 1
    - `grep -c "## Security Considerations" NUB-RELAY.md` returns 1
    - `grep -c '"ok": true' NUB-RELAY.md` returns 1 or more (publish success example)
    - `grep -c '"ok": false' NUB-RELAY.md` returns 1 or more (publish error example)
    - `grep -c "NostrFilter" NUB-RELAY.md` returns 2 or more (table, filter description)
    - Section order in file: Description appears before API Surface, API Surface before Wire Protocol, Wire Protocol before Shell Behavior, Shell Behavior before Security Considerations
  </acceptance_criteria>
  <done>NUB-RELAY.md on the nub-relay branch is fully rewritten with relay.* message types, JSON wire protocol examples, signed NostrEvent publish path, no NIP-01 wire verbs or kind numbers, and section order matches NUB-STORAGE pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Force-push to PR branch and update PR body</name>
  <files>NUB-RELAY.md</files>
  <read_first>
    - NUB-RELAY.md (the rewritten file from Task 1, confirm it exists on branch)
    - `gh pr view 2 --json title,body` (current PR metadata)
  </read_first>
  <action>
    Commit the rewritten NUB-RELAY.md on the `nub-relay` branch and force-push to update PR #2.

    1. Stage and commit NUB-RELAY.md on the `nub-relay` branch with message:
       `docs(nub-relay): rewrite spec to use SPEC.md wire format`

       The commit message body should say: "Replace NIP-01 wire verbs and kind 29001 scoped relay events with relay.* typed messages per NIP-5D wire format. publish() now accepts a signed NostrEvent — napplets sign via window.nostr.signEvent() before publishing. API surface (subscribe/publish/query) preserved."

    2. Force-push the `nub-relay` branch to origin:
       `git push --force-with-lease origin nub-relay`

    3. Update PR #2 title and body using `gh pr edit 2`:

       Title: `NUB-RELAY: Relay proxy interface`
       (Remove "NIP-01" from old title — the spec no longer uses NIP-01 wire verbs)

       Body:
       ```
       **Namespace:** `window.napplet.relay`
       **Status:** draft
       **Discovery:** `shell.supports("relay")`

       ## Summary

       NUB-RELAY provides napplets with relay access through the shell. Sandboxed iframes cannot open WebSocket connections directly. The shell acts as a relay proxy, forwarding publish and subscribe operations to its connected relay pool and delivering matching events back to the napplet. Napplets sign events themselves via `window.nostr.signEvent()` (NIP-07) before publishing.

       ## Wire Format

       Uses NIP-5D typed messages (`relay.subscribe`, `relay.publish`, `relay.query`, `relay.close`) with `relay.event`/`relay.eose`/`relay.closed` delivery and `relay.publish.result`/`relay.query.result` responses. Correlation via `id` and `subId` fields.

       ## References

       - [NIP-5D](https://github.com/nostr-protocol/nips/blob/master/5D.md) -- Core napplet protocol
       - [SPEC.md](SPEC.md) -- NIP-5D wire format and NUB framework
       ```

    4. Switch back to master branch after pushing:
       `git checkout master`
  </action>
  <verify>
    <automated>gh pr view 2 --json title,state,headRefName 2>/dev/null && echo "---" && git log --oneline nub-relay | head -3 && echo "---" && git branch --show-current</automated>
  </verify>
  <acceptance_criteria>
    - `gh pr view 2 --json state` shows `"state":"OPEN"`
    - `gh pr view 2 --json title` contains "Relay proxy interface" and does NOT contain "NIP-01"
    - `git log --oneline nub-relay | head -1` contains "rewrite spec to use SPEC.md wire format"
    - `gh pr view 2 --json body` contains "relay.subscribe" and "relay.publish"
    - `gh pr view 2 --json body` does NOT contain "kind 29001" or "REQ/EVENT/CLOSE"
    - `gh pr view 2 --json body` contains "window.nostr.signEvent"
    - `git branch --show-current` returns `master` (back on master after push)
  </acceptance_criteria>
  <done>PR #2 is updated with the rewritten NUB-RELAY spec. The nub-relay branch has the new commit force-pushed. PR title updated to remove NIP-01 reference. Working tree is back on master.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `git show nub-relay:NUB-RELAY.md | grep -c "relay\."` returns 20+ (many message type references)
2. `git show nub-relay:NUB-RELAY.md | grep -ci '29001\|\["REQ"\|\["EVENT"\|NUB-SIGNER\|EventTemplate'` returns 0
3. `git show nub-relay:NUB-RELAY.md | grep "## "` shows sections in order: Description, API Surface, Wire Protocol, Shell Behavior, Security Considerations
4. `git show nub-relay:NUB-RELAY.md | grep -c "window\.nostr\.signEvent"` returns 2 or more
5. `gh pr view 2 --json state` shows OPEN
6. Working branch is master
</verification>

<success_criteria>
- NUB-RELAY.md on `nub-relay` branch defines relay.subscribe, relay.publish, relay.query, relay.close, relay.event, relay.eose, relay.closed, relay.publish.result, relay.query.result message types
- relay.publish accepts a signed NostrEvent — napplets sign via window.nostr.signEvent() first
- Wire Protocol section has concrete JSON examples with correlation IDs (id) and subscription IDs (subId)
- Scoped relay support preserved via optional relay field
- No NIP-01 wire verbs, kind 29001, NUB-SIGNER, EventTemplate, or delegated key references
- Shell Behavior describes relay forwarding, signature verification, ACL, pool management
- Security Considerations covers signature verification, ACL, SSRF for scoped relays, filter resource exhaustion
- Section order matches NUB-STORAGE pattern: Description, API Surface, Wire Protocol, Shell Behavior, Security Considerations
- PR #2 title and body updated to reflect new wire format
- Working tree back on master
</success_criteria>

<output>
After completion, create `.planning/phases/04-nub-relay-rewrite/04-01-SUMMARY.md`
</output>
