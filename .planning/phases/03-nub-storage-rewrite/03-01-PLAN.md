---
phase: 03-nub-storage-rewrite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [NUB-STORAGE.md]
autonomous: true
requirements: [SPEC-02]

must_haves:
  truths:
    - "NUB-STORAGE.md uses storage.* message types with JSON payloads — no kind 29003 or IFC_PEER references"
    - "Wire Protocol section defines request/response shapes matching SPEC.md format: { type: 'storage.get', id, key } / { type: 'storage.get.result', id, value }"
    - "No references to signing, keypairs, pubkeys, kind numbers, or cryptographic operations in napplet-facing sections"
    - "Shell Behavior describes composite key scoping and quota enforcement without exposing internal event mechanics"
    - "PR #3 on GitHub reflects the rewritten spec"
  artifacts:
    - path: "NUB-STORAGE.md"
      provides: "Rewritten NUB-STORAGE spec using SPEC.md wire format"
      contains: "storage.get"
  key_links:
    - from: "NUB-STORAGE.md Wire Protocol"
      to: "NUB-STORAGE.md API Surface"
      via: "message type names mirror API methods"
      pattern: "storage\\.(get|set|remove|keys)"
    - from: "NUB-STORAGE.md Wire Protocol"
      to: "SPEC.md wire format"
      via: "domain.action pattern with .result suffix"
      pattern: "storage\\..*\\.result"
---

<objective>
Rewrite NUB-STORAGE spec on the `nub-storage` PR branch to replace the kind 29003/IFC_PEER wire format with SPEC.md's `{ "type": "storage.*", ...payload }` format. The API surface (getItem/setItem/removeItem/keys) is preserved. This is the first spec rewrite and validates the new structure for all subsequent phases.

Purpose: Align NUB-STORAGE with the two-layer architecture from SPEC.md, proving the wire format pattern works before applying it to higher-complexity specs (RELAY, NOSTRDB, IFC, PIPES).
Output: Rewritten NUB-STORAGE.md force-pushed to `nub-storage` branch (PR #3), PR body updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-nub-storage-rewrite/03-CONTEXT.md
@SPEC.md

<interfaces>
<!-- SPEC.md wire format pattern (the canonical pattern this rewrite must follow): -->

Wire format: { "type": "<domain>.<action>", ...payload }
Response pattern: { "type": "<domain>.<action>.result", "id": "<correlation>", ...result }
Example from SPEC.md:
  Request:  { "type": "foo.bar", "id": "abc", "data": {...} }
  Response: { "type": "foo.bar.result", "id": "abc", "result": {...} }

Identity: shell maps MessageEvent.source to (dTag, aggregateHash) at iframe creation
Discovery: shell.supports("storage")
Namespace: window.napplet.storage
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite NUB-STORAGE.md on nub-storage branch</name>
  <files>NUB-STORAGE.md</files>
  <read_first>
    - SPEC.md (wire format pattern, identity model)
    - `git show nub-storage:NUB-STORAGE.md` (existing spec to rewrite)
    - .planning/phases/03-nub-storage-rewrite/03-CONTEXT.md (locked decisions D-01 through D-09)
  </read_first>
  <action>
    Check out the `nub-storage` branch. Rewrite `NUB-STORAGE.md` completely. The new spec preserves the API surface but replaces all wire format content.

    **Header (preserve structure, update metadata):**
    - Keep: NUB-STORAGE title, `draft` status, NUB ID, Namespace (`window.napplet.storage`), Discovery (`shell.supports("storage")`)

    **Description section:**
    - Preserve the existing paragraph explaining why napplets need proxied storage (opaque origins, no localStorage). Keep the composite key `(dTag, aggregateHash)` scoping explanation.
    - Remove any mention of IFC_PEER or kind 29003.

    **API Surface section (per D-05):**
    - Keep the existing TypeScript interface block with all four methods: `getItem(key: string): Promise<string | null>`, `setItem(key: string, value: string): Promise<void>`, `removeItem(key: string): Promise<void>`, `keys(): Promise<string[]>`.
    - Add inline comments showing which message type each method maps to:
      ```typescript
      interface NappletStorage {
        getItem(key: string): Promise<string | null>;    // via storage.get
        setItem(key: string, value: string): Promise<void>; // via storage.set
        removeItem(key: string): Promise<void>;           // via storage.remove
        keys(): Promise<string[]>;                        // via storage.keys
      }
      ```
    - Keep the brief method descriptions below the interface block.
    - Update the async explanation: "All methods are async because they cross the postMessage boundary. Each request includes a correlation ID (`id`); the shell responds with a matching ID so the napplet can resolve the correct Promise."

    **Wire Protocol section (replaces "Event Kinds" — per D-01, D-02, D-03, D-04):**
    - Section title: `## Wire Protocol`
    - Opening sentence: "Storage operations use the NIP-5D wire format. Each request includes an `id` field for correlation."
    - Define these message types as a table:

      | Type | Direction | Payload fields |
      |------|-----------|----------------|
      | `storage.get` | napplet -> shell | `id`, `key` |
      | `storage.set` | napplet -> shell | `id`, `key`, `value` |
      | `storage.remove` | napplet -> shell | `id`, `key` |
      | `storage.keys` | napplet -> shell | `id` |
      | `storage.get.result` | shell -> napplet | `id`, `value` (string or null) |
      | `storage.set.result` | shell -> napplet | `id` |
      | `storage.remove.result` | shell -> napplet | `id` |
      | `storage.keys.result` | shell -> napplet | `id`, `keys` (string array) |

    - After the table, show concrete JSON examples for at least two operations (get and set):

      **Get:**
      ```
      -> { "type": "storage.get", "id": "a1", "key": "theme" }
      <- { "type": "storage.get.result", "id": "a1", "value": "dark" }
      ```

      **Set:**
      ```
      -> { "type": "storage.set", "id": "b2", "key": "theme", "value": "light" }
      <- { "type": "storage.set.result", "id": "b2" }
      ```

    - Per D-04 and D-08, document error handling: "Any result message MAY include an `error` field (string). When `error` is present, other result fields are undefined." Show one error example:
      ```
      <- { "type": "storage.set.result", "id": "b2", "error": "quota exceeded" }
      ```

    - `value` in `storage.get.result` is `null` when the key does not exist (matching localStorage semantics). Document this.

    **Shell Behavior section (per D-07, updated):**
    - Keep these MUST requirements:
      - The shell MUST scope storage by composite key `(dTag, aggregateHash)`. Different napplet types and different versions of the same napplet MUST have isolated storage.
      - The shell MUST enforce a per-napplet storage quota. A recommended default is 512 KB measured in UTF-8 byte count.
      - The shell MUST persist storage so data survives page reloads. How the shell stores data (localStorage, IndexedDB, etc.) is an implementation detail.
      - The shell MUST respond to every request with a result message carrying the same `id`.
      - The shell MUST return an `error` field in the result message for quota violations and invalid requests.
    - Remove the backward-compatible migration bullet per D-09.
    - Remove any reference to kind numbers, IFC_PEER, or event-based transport.
    - Describe composite key scoping without exposing how the shell internally constructs the key. Say "The shell maps each napplet's `(dTag, aggregateHash)` identity to an isolated storage namespace" — not "the shell prefixes localStorage keys with dTag:hash:".

    **Security Considerations section (updated):**
    - Keep: storage scoped by composite key, napplet isolation, quota enforcement, strings-only values, shell MAY enforce ACL.
    - Remove the "validate storage key prefixes" bullet — this is an implementation detail of how the shell scopes; the spec should say the shell MUST enforce isolation, not prescribe the mechanism.
    - Keep: "Storage keys and values are visible to the shell. Napplets that need confidential storage should encrypt values before storing."
    - Do NOT add any signing, keypair, or crypto operations to this section.

    **Remove these sections entirely:**
    - "Event Kinds" table (replaced by Wire Protocol)
    - "Response Tags" table (replaced by JSON payload fields)
    - "Implementations" section (no implementations exist yet)

    **Section ordering per D-06:** Description -> API Surface -> Wire Protocol -> Shell Behavior -> Security Considerations

    The file must NOT contain:
    - Any reference to kind 29003, IFC_PEER, or NIP-01 event kinds
    - Any reference to `t` tags, topic tags, or tag-based routing
    - Any reference to signing, keypairs, pubkeys, or `window.nostr`
    - The word "backward-compatible" or migration guidance
    - An "Implementations" section
  </action>
  <verify>
    <automated>git diff nub-storage -- NUB-STORAGE.md | head -200 && echo "---" && grep -c "storage\\.get" NUB-STORAGE.md && echo "---" && grep -c "29003\|IFC_PEER\|kind\|signEvent\|keypair\|pubkey" NUB-STORAGE.md || echo "0 forbidden terms found"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "storage\.get" NUB-STORAGE.md` returns 3 or more (table row, example, result type)
    - `grep -c "storage\.set" NUB-STORAGE.md` returns 3 or more
    - `grep -c "storage\.remove" NUB-STORAGE.md` returns 2 or more
    - `grep -c "storage\.keys" NUB-STORAGE.md` returns 2 or more
    - `grep -c "\.result" NUB-STORAGE.md` returns 4 or more (one per operation type)
    - `grep -ci "29003\|IFC_PEER\|kind 29003" NUB-STORAGE.md` returns 0
    - `grep -ci "signEvent\|keypair\|pubkey\|window\.nostr" NUB-STORAGE.md` returns 0
    - `grep -c "backward.compatible\|migration" NUB-STORAGE.md` returns 0
    - `grep -c "## Wire Protocol" NUB-STORAGE.md` returns 1
    - `grep -c "## Event Kinds" NUB-STORAGE.md` returns 0
    - `grep -c "quota exceeded" NUB-STORAGE.md` returns 1 or more (error example)
    - `grep -c '"id"' NUB-STORAGE.md` returns 3 or more (correlation ID in examples)
    - Section order in file: Description appears before API Surface, API Surface before Wire Protocol, Wire Protocol before Shell Behavior, Shell Behavior before Security Considerations
  </acceptance_criteria>
  <done>NUB-STORAGE.md on the nub-storage branch is fully rewritten with storage.* message types, JSON wire protocol examples, no kind numbers or crypto references, and the section order matches D-06.</done>
</task>

<task type="auto">
  <name>Task 2: Force-push to PR branch and update PR body</name>
  <files>NUB-STORAGE.md</files>
  <read_first>
    - NUB-STORAGE.md (the rewritten file from Task 1, confirm it exists on branch)
    - `gh pr view 3 --json title,body` (current PR metadata)
  </read_first>
  <action>
    Commit the rewritten NUB-STORAGE.md on the `nub-storage` branch and force-push to update PR #3.

    1. Stage and commit NUB-STORAGE.md on the `nub-storage` branch with message:
       `docs(nub-storage): rewrite spec to use SPEC.md wire format`

       The commit message body should say: "Replace kind 29003/IFC_PEER event transport with storage.* typed messages per NIP-5D wire format. API surface (getItem/setItem/removeItem/keys) preserved."

    2. Force-push the `nub-storage` branch to origin:
       `git push --force-with-lease origin nub-storage`

    3. Update PR #3 body using `gh pr edit 3 --body`:
       ```
       **Namespace:** `window.napplet.storage`
       **Status:** draft
       **Discovery:** `shell.supports("storage")`

       ## Summary

       NUB-STORAGE provides napplets with an async localStorage-like API. Without `allow-same-origin`, iframes have opaque origins and cannot access `localStorage` directly. This interface routes storage operations through the shell via `postMessage`, scoping data by napplet identity -- a composite key of `(dTag, aggregateHash)` -- to enforce isolation between napplets.

       ## Wire Format

       Uses NIP-5D typed messages (`storage.get`, `storage.set`, `storage.remove`, `storage.keys`) with `.result` response suffix and correlation IDs.

       ## References

       - [NIP-5D](https://github.com/nostr-protocol/nips/blob/master/5D.md) -- Core napplet protocol
       - [SPEC.md](SPEC.md) -- NIP-5D wire format and NUB framework
       ```

    4. Switch back to master branch after pushing:
       `git checkout master`
  </action>
  <verify>
    <automated>gh pr view 3 --json title,state,headRefName 2>/dev/null && echo "---" && git log --oneline nub-storage | head -3 && echo "---" && git branch --show-current</automated>
  </verify>
  <acceptance_criteria>
    - `gh pr view 3 --json state` shows `"state":"OPEN"`
    - `git log --oneline nub-storage | head -1` contains "rewrite spec to use SPEC.md wire format"
    - `gh pr view 3 --json body` contains "storage.get" and "storage.set"
    - `gh pr view 3 --json body` does NOT contain "IPC-PEER" or "kind 29003"
    - `git branch --show-current` returns `master` (back on master after push)
    - `git log nub-storage --oneline | wc -l` shows the rewrite commit exists on top
  </acceptance_criteria>
  <done>PR #3 is updated with the rewritten NUB-STORAGE spec. The nub-storage branch has the new commit force-pushed. Working tree is back on master.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `git show nub-storage:NUB-STORAGE.md | grep -c "storage\."` returns 10+ (multiple message type references)
2. `git show nub-storage:NUB-STORAGE.md | grep -c "29003\|IFC_PEER"` returns 0
3. `git show nub-storage:NUB-STORAGE.md | grep "## "` shows sections in order: Description, API Surface, Wire Protocol, Shell Behavior, Security Considerations
4. `gh pr view 3 --json state` shows OPEN
5. Working branch is master
</verification>

<success_criteria>
- NUB-STORAGE.md on `nub-storage` branch defines `storage.get`, `storage.set`, `storage.remove`, `storage.keys` message types with `.result` response suffix
- Wire Protocol section has concrete JSON examples with correlation IDs
- No kind numbers, IFC_PEER, signing, or crypto references in the spec
- Shell Behavior describes composite key scoping and 512KB quota without exposing internals
- PR #3 body updated to reflect new wire format
- Working tree back on master
</success_criteria>

<output>
After completion, create `.planning/phases/03-nub-storage-rewrite/03-01-SUMMARY.md`
</output>
